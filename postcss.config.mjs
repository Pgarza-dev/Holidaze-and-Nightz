/** @type {import('postcss-load-config').Config} */
const config = {
  plugins: {
    // 'tailwindcss/nesting': { 'postcss-nesting': true },
    tailwindcss: {},
    // autoprefixer: {},
  },
}

export default config
